// FILE: lib/config/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales - inspirées de l'image
  static const Color primary = Color(0xFF1A3A6B);     // Bleu foncé
  static const Color secondary = Color(0xFFE65100);   // Orange
  static const Color accent = Color(0xFFFF9800);      // Orange clair
  
  // Neutres
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF2E7D32);
  
  // Texte
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Colors.white;
  
  // Status
  static const Color available = Color(0xFF4CAF50);
  static const Color limited = Color(0xFFFF9800);
}